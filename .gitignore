# Compiled class file
*.class

# Log file
*.log

# BlueJ files
*.ctxt

# Mobile Tools for Java (J2ME)
.mtj.tmp/

# Maven build output
target/

# Package Files #
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*
replay_pid*

# Node tooling for Probity TDD guard (dev-only; @nizos/probity is a local devDependency).
# package.json and probity.config.mjs are tracked (reproducible setup + TDD policy).
node_modules/
package-lock.json

# Personal Claude Code settings — .claude/settings.json is the shared/tracked config.
.claude/settings.local.json

# Phase 4: raw corpus sources (Wiktionary dump, UVW-2026) — multi-GB, offline inputs to the
# dicts compile pipeline. Only the compiled resources under dicts/src/main/resources are tracked.
vietnamese-tokenizer-dicts/raw-corpus/

# Phase 4: compiled output of the one-off dicts generator (not wired into any pom.xml build)
vietnamese-tokenizer-dicts/generator/out/
